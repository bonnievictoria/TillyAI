# Default `npm run dev`: no requests to the backend — see src/lib/api.ts (isFrontendOnlyMode).
# Override locally in `.env.development.local` with VITE_FRONTEND_ONLY=false if you want the proxy + real API.
VITE_FRONTEND_ONLY=true
